<!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h2>You are going to edit Username of <%= user.email %></h2>
    <form action="/user/<%= user.id %>/edit?_method=PATCH" method="post">
        <textarea name="username" cols="25" rows="2"><%=user.username%></textarea>
        <input type="password" name="password" placeholder="Enter password" required>
        <button type="submit">Update</button>
    </form>
</body>
</html> -->

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Profile</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        body {
            background: #f0f2f5;
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .edit-container {
            background: white;
            width: 100%;
            max-width: 500px;
            padding: 30px;
            border-radius: 12px;
            box-shadow: 0 8px 24px rgba(0,0,0,0.1);
        }

        .alert-box {
            background-color: #e3f2fd;
            border-left: 5px solid #2196f3;
            padding: 15px;
            margin-bottom: 25px;
            border-radius: 4px;
        }

        .alert-box p {
            color: #0d47a1;
            font-size: 0.95rem;
            line-height: 1.4;
        }

        .alert-box strong {
            display: block;
            margin-top: 4px;
            word-break: break-all;
        }

        .form-group {
            margin-bottom: 20px;
        }

        label {
            display: block;
            font-weight: 600;
            margin-bottom: 8px;
            color: #444;
            font-size: 0.9rem;
        }

        input {
            width: 100%;
            padding: 12px;
            border: 1px solid #ddd;
            border-radius: 6px;
            font-size: 1rem;
            transition: border-color 0.3s, box-shadow 0.3s;
        }

        input:focus {
            outline: none;
            border-color: #2196f3;
            box-shadow: 0 0 0 3px rgba(33, 150, 243, 0.1);
        }

        .button-group {
            display: flex;
            gap: 12px;
            margin-top: 10px;
        }

        .btn {
            flex: 1;
            padding: 12px;
            border: none;
            border-radius: 6px;
            font-weight: 600;
            cursor: pointer;
            font-size: 1rem;
            transition: 0.2s;
        }

        .btn-update {
            background-color: #2ecc71;
            color: white;
        }

        .btn-update:hover {
            background-color: #27ae60;
        }

        .btn-cancel {
            background-color: #f8f9fa;
            color: #666;
            text-decoration: none;
            text-align: center;
            border: 1px solid #ddd;
        }

        .btn-cancel:hover {
            background-color: #eee;
        }
    </style>
</head>
<body>

<div class="edit-container">
    <div class="alert-box">
        <p>You are editing the account for: 
            <strong><%= user.email %></strong>
        </p>
    </div>

    <form action="/user/<%= user.id %>?_method=PATCH" method="post">
        <div class="form-group">
            <label>Current Username</label>
            <input type="text" name="username" value="<%=user.username%>">
        </div>

        <div class="form-group">
            <label>Verify Password to Confirm Changes</label>
            <input type="password" name="password" placeholder="Enter password" required>
        </div>

        <div class="button-group">
            <button type="submit" class="btn btn-update">Update Profile</button>
            <a href="/users" class="btn btn-cancel">Cancel</a>
        </div>
    </form>
</div>

</body>
</html>