<!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Users</title>
    <style>
        th, td{
            border: 2px solid black;
        }
    </style>
</head>
<body>
    <table>
        <tr>
            <th>ID</th>
            <th>Username</th>
            <th>Email</th>
        </tr>
        <% for( let user of result) { %>
        <tr>
            <td><%= user.id%></td>
            <td><%= user.username%></td>
            <td><%= user.email%></td>
            <td>
                <form action="/user/<%= user.id %>" method="get">
                    <button >Edit username</button>
                </form>
            </td>
        </tr>
        <% } %>
    </table>
</body>
</html> -->

<!-- Design by AI -->
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Management</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', sans-serif;
        }

        body {
            background-color: #f8f9fa;
            padding: 40px 20px;
            color: #333;
        }

        .container {
            max-width: 1100px;
            margin: 0 auto;
            background: white;
            padding: 20px;
            border-radius: 12px;
            box-shadow: 0 4px 20px rgba(0,0,0,0.08);
        }

        .header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 25px;
            padding: 0 10px;
        }

        h2 { color: #2d3436; }

        /* Table Wrapper for Horizontal Scrolling */
        .table-wrapper {
            overflow-x: auto;
            border-radius: 8px;
            border: 1px solid #eee;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            text-align: left;
            min-width: 800px; /* Prevents squishing */
        }

        th {
            background-color: #f1f4f8;
            color: #4b5563;
            font-weight: 600;
            padding: 16px;
            font-size: 0.9rem;
            text-transform: uppercase;
            letter-spacing: 0.05em;
        }

        td {
            padding: 14px 16px;
            border-bottom: 1px solid #f1f1f1;
            font-size: 0.95rem;
            color: #4a4a4a;
        }

        /* Zebra Striping */
        tr:nth-child(even) { background-color: #fafbfc; }

        tr:hover { background-color: #f0f7ff; }

        /* ID Column special styling */
        .id-cell {
            font-family: 'Courier New', monospace;
            font-size: 0.8rem;
            color: #888;
            max-width: 150px;
            overflow: hidden;
            text-overflow: ellipsis;
            white-space: nowrap;
        }

        /* Action Buttons */
        .btn-edit {
            padding: 6px 12px;
            background-color: #fff;
            border: 1px solid #0984e3;
            color: #0984e3;
            border-radius: 6px;
            cursor: pointer;
            font-size: 0.85rem;
            transition: 0.2s;
        }

        .btn-edit:hover {
            background-color: #0984e3;
            color: white;
        }

        #delete-btn {
            border: 1px solid #e30909;
            color: #e30909;
        }

        #delete-btn:hover {
            background-color: #e30909;
            color: white;
        }
    </style>
</head>
<body>

<div class="container">
    <div class="header">
        <h2>Registered Users</h2>
        <a href="/user" target="_blank" style="color: #0984e3; text-decoration: none; font-weight: 600;">+ Add New</a>
    </div>

    <div class="table-wrapper">
        <table>
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Username</th>
                    <th>Email Address</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                <% for( let user of result) { %>
                <tr>
                    <td class="id-cell"><%= user.id%></td>
                    <td><strong><%= user.username%></strong></td>
                    <td><%= user.email %></td>
                    <td>
                        <form action="/user/<%= user.id %>" method="get">    
                            <button class="btn-edit">Edit User</button>
                        </form><br>
                        <a href="/delete/<%= user.id %>"><button class="btn-edit" id="delete-btn">Delete</button></a>
                    </td>
                </tr>
                <% } %>
                </tbody>
        </table>
    </div>
</div>

</body>
</html>