<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit post</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <h1>Edit your post</h1>
    <div>
        <h3>ID : <%= post.id %></h3>
        <h3>Username : @<%= post.Username %></h3>
    </div>
    <!-- here, we are overriding method because form only accepts post & get request and we have to send patch request.We write it like query string in action attribute. it overrides post method and sends Patch method -->
    <form action="/posts/<%= post.id %>?_method=PATCH", method="post">
        <textarea name="content" id="" cols="50" rows="15"><%= post.content %></textarea>&nbsp;&nbsp;&nbsp;
        <button type="submit">Update</button>
    </form><br>
    <a href="/posts"><button>Back to home</button></a>
</body>
</html>