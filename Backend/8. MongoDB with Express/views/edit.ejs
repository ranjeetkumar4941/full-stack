<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Chat</title>
    <style>
        /* Specific styling for the Edit Chat page */
        .edit-container {
            max-width: 600px;
            margin: 40px auto;
            background: #ffffff;
            padding: 2.5rem;
            border-radius: 16px;
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.05);
            border: 1px solid #e2e8f0;
        }

        .id-badge {
            display: inline-flex;
            align-items: center;
            background-color: #f1f5f9;
            color: #475569;
            padding: 6px 12px;
            border-radius: 6px;
            font-family: 'JetBrains Mono', monospace;
            font-size: 0.8rem;
            margin-top: 8px;
            margin-bottom: 24px;
            border: 1px dashed #cbd5e1;
        }

        .id-label {
            text-transform: uppercase;
            font-weight: 700;
            margin-right: 8px;
            color: #94a3b8;
        }

        /* --- MODIFIED TEXTAREA DESIGN --- */
        .edit-textarea {
            width: 100%;
            /* Ensures it fills the container */
            min-height: 180px;
            /* Slightly taller for better readability */
            padding: 16px;
            /* Breathing room for the text */
            line-height: 1.6;
            border-radius: 12px;
            /* Rounded corners to match the container */
            border: 2px solid #e2e8f0;
            /* Soft border */
            background-color: #fcfcfd;
            /* Very subtle off-white background */
            font-family: inherit;
            /* Uses the body font */
            font-size: 1rem;
            color: #1e293b;
            resize: vertical;
            /* Prevents breaking the layout horizontally */
            transition: all 0.2s ease-in-out;
            /* Smooth transition for focus */
            outline: none;
            /* Removes default browser outline */
        }

        /* Focus state: The "Glow" effect when the user clicks to type */
        .edit-textarea:focus {
            border-color: #4f46e5;
            /* Matches your Update button color */
            background-color: #ffffff;
            box-shadow: 0 0 0 4px rgba(79, 70, 229, 0.1);
            /* Indigo halo */
        }

        /* Hover state: Subtle border change */
        .edit-textarea:hover {
            border-color: #cbd5e1;
        }

        /* -------------------------------- */

        .edit-actions {
            margin-top: 2rem;
            padding-top: 1.5rem;
            border-top: 1px solid #f1f5f9;
        }

        .btn-update {
            background-color: #4f46e5;
            color: white;
            width: 100%;
            padding: 14px;
            /* Slightly thicker for a better click target */
            font-size: 1rem;
            font-weight: 600;
            /* Bold text for the main action */
            border-radius: 10px;
            border: none;
            cursor: pointer;
            transition: background 0.2s;
        }

        .btn-update:hover {
            background-color: #4338ca;
        }

        .back-link {
            display: block;
            text-align: center;
            margin-top: 1.2rem;
            color: #64748b;
            text-decoration: none;
            font-size: 0.9rem;
        }

        .back-link:hover {
            color: #4f46e5;
            text-decoration: underline;
        }
    </style>
</head>

<body>

    <div class="edit-container">
        <header>
            <h1>Edit your chat</h1>
            <div class="id-badge">
                <span class="id-label">Chat ID:</span>
                <%= chat._id %>
            </div>
        </header>

        <form action="/chats/<%= chat._id %>?_method=PATCH" method="post">
            <div class="form-group">
                <label for="msg">Message Content</label>
                <textarea id="msg" name="msg" class="form-control edit-textarea"><%= chat.msg %></textarea>
            </div>

            <div class="edit-actions">
                <button type="submit" class="btn btn-update">Update Message</button>
                <a href="/chats" class="back-link">← Cancel and go back</a>
            </div>
        </form>
    </div>
</body>

</html>